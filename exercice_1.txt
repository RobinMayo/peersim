EXERCICE I

Question 1 :

function requestCS(Node host)
begin
	EDSimulator.add(request_cs)
	
	IF (last not equal nil)
	begin
		SEND(host, dest, new Message(), protocol_id)
		last = nil
		return
	end
end